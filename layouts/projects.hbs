<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{{title}}</title>
    <link href="../output.css" rel="stylesheet" />
    <meta name="description" content="{{description}}" />
    <script
      src="../components/navigation.js"
      type="text/javascript"
      defer
    ></script>
    <script
      src="../components/infoBox.js"
      type="text/javascript"
    ></script>
  </head>

  <body>
    <navigation-component></navigation-component>
    <h1 class="text-grey">{{{title}}}</h1>
    <div
      class="bg-cover bg-center p-4"
      style="background-image: url({{hero-img}})"
    >
      <div class="md:container md:mx-auto p-4 bg-white opacity-75">
        <div class="mt-4 mb-6">
          <div class="mb-3 text-xl font-bold">{{{project-summary.header}}}</div>
          <div
            class="text-sm text-neutral-600"
          >{{{project-summary.message}}}</div>
        </div>
      </div>
    </div>

<div class="flex p-4">
<div class="container mx-auto bg-white">
  <p>{{info-message.title}} </p>
  <p>{{info-message.notes}} </p>
  <infobox-component 
  title={{info-message.title}}
  notes={{info-message.notes}}>
  </infobox-component>
  <p>something else below</p>
</div>
</div>
</body>
</html>